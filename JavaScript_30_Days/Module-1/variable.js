console.log("variable js")

//Varialbe

let name = "Bikrom Adatya Roy";
name = "Sporsho singha";
console.log(name);

const age = 25;
//age = 23;  //const er value change hoy nah jar karone value dile o error show korbe.
console.log(age);

const isAdult = true;

//Declear typeof() funciton ....
console.log(typeof(name), typeof(age), typeof(isAdult));

// How to remember all the data types of java Scripts
/**
 * s = stringd
 * s = symble
 * b = bigInt
 * b = boolean
 * n = number
 * n = null
 * u = undefine
 * o = object
*/
