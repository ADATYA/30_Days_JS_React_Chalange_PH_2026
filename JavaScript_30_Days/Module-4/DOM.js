/* 
What is DOM?
= > **DOM (Document Object Model)** হলো Browser-এর তৈরি করা HTML document-এর একটি **tree/object representation**। JavaScript DOM-এর মাধ্যমে webpage-এর HTML element **select, change, add, delete এবং event handle** করতে পারে। DOM-এর প্রধান object হলো `document`। Element select করতে `getElementById()`, `querySelector()` ও `querySelectorAll()` ব্যবহার করা হয়। Content পরিবর্তনে `textContent` ও `innerHTML`, CSS পরিবর্তনে `style` ও `classList`, নতুন element তৈরিতে `createElement()` এবং event handle করতে `addEventListener()` ব্যবহার করা হয়।
**Flow:** HTML → Browser → DOM Tree → JavaScript → Webpage Update

*** DOM = JavaScript দিয়ে HTML webpage-কে control করার bridge.
*/